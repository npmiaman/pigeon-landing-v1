/**
 * The one place the site's outbound links live.
 *
 * This archive has no backend, so the waiting-list form is gone and every
 * "get started" on the page ends at the same two places: a calendar and an
 * inbox. Keeping them here means changing the calendar link is one edit, not
 * five.
 */
export const CALENDLY = 'https://calendly.com/aman-trypigeon/30min'
export const EMAIL = 'aman@trypigeon.in'
